spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:5432/postgres
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD}
  jpa:
    show-sql:true

weather:
  mapquest:
    api:
      key: ${MAPQUEST_API_KEY}
  location-radius: ${LOCATION_RADIUS:5}
